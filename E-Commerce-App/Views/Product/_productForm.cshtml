@model E_Commerce_App.Models.Product


@*{
    var cat = ViewBag.Categories;
}*@
<form method="post" asp-controller="Product" asp-action="@(Model.Id > 0 ? "Edit" : "Add")" asp-route-productId="@Model.Id" enctype="multipart/form-data">
    @if (Model.Id > 0)
    {
        <input type="hidden" asp-for="Id" />
    }

    <div class="form-group">
        <label asp-for="Name">Product Name</label>
        <input asp-for="Name" class="form-control" />
    </div>

    <div class="form-group">
        <label asp-for="ExpiryDate">Expiry Date</label>
        <input asp-for="ExpiryDate" class="form-control" />
    </div>

    <div class="form-group">
        <label asp-for="Price">Price</label>
        <input asp-for="Price" class="form-control" />
    </div>

    <div class="form-group">
        <label asp-for="Description">Description</label>
        <input asp-for="Description" class="form-control" />
    </div>

    <div class="form-group">
        <label asp-for="CategoryId">Category</label>
        <input asp-for="CategoryId" class="form-control" />
    </div>



 
    <button type="submit" class="btn btn-primary">
        @if (Model.Id > 0)
        {
            <text>Save</text>
        }
        else
        {
            <text>Add</text>
        }
    </button>
    <div asp-validation-summary="All" class="text-danger"></div>
    <div>
   
<form method="post" asp-action="Upload" asp-controller="Product" enctype="multipart/form-data">
    <div>
      
        <input type="file" name="file" />

                <button class="btn btn-outline-dark">Upload the file</button>
    </div>

</form>
    </div>
</form>
@* @if(Model != null)
{
    <div>Uploaded File</div>
    <ul>
        <li>Name : @Model.Name</li>
        <li>Type : @Model.Type</li>
        <li>Size : @Model.Size</li>
        <li>URL : @Model.Url</li>
    </ul>
    if(Model.Url != null)
    {
        <img src ="@Model.Url" width="250" alt="Product image" />
    }
} *@